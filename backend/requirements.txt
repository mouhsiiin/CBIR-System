# Web Framework
flask
flask-restful
flask-cors

# Computer Vision & Image Processing
# Note: Using opencv-python-headless saves space by excluding GUI dependencies
opencv-python-headless
scikit-image
Pillow
 
# Data Science & ML
numpy
scikit-learn
scipy

# Object Detection / YOLO
# We install the CPU version of torch BEFORE ultralytics to prevent the 4GB+ Nvidia download
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
ultralytics